/**
 * Command-line entry points for running polyglot contract generation outside the Maven plugin.
 *
 * <p>Most Maven users should prefer the plugin goals; the CLI is useful for local diagnostics and
 * integration tests around generated contract output.
 */
package io.github.ih0rd.codegen.cli;
