/**
 * Type mapping support used when rendering Java interfaces from guest-language annotations.
 *
 * <p>Mappings are intentionally conservative so generated APIs stay predictable across releases.
 */
package io.github.ih0rd.codegen.types;
