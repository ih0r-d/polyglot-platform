/**
 * Script source implementations used by the runtime adapter.
 *
 * <p>These types define where guest-language scripts are loaded from. The built-in sources cover
 * classpath resources, filesystem paths, in-memory scripts, and ordered fallback chains.
 */
package io.github.ih0rd.adapter.spi;
