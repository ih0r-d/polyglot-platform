/**
 * Utility types retained for compatibility with older adapter usage.
 *
 * <p>This package is not the recommended entry point for new integrations. Prefer the executor,
 * script source, and Spring starter APIs for supported runtime behavior.
 */
package io.github.ih0rd.adapter.utils;
